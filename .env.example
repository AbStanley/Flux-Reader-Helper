# URL for your Ollama instance.
# When running in Docker and accessing from another device (e.g. phone),
# use your computer's local IP address (e.g., http://192.168.1.5:11434).
# Do NOT use localhost if you want to access it from other devices.
VITE_OLLAMA_URL=http://localhost:11434